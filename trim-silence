for file in ./*.wav
do
    ffmpeg -y -i "$file" -af silenceremove=stop_periods=1:detection=peak:stop_threshold=0.0001 "${file%.*}-temp.wav"
    ffmpeg -y -i "${file%.*}-temp.wav" -af "atrim=0.025" "$file"
    rm "${file%.*}-temp.wav"
done