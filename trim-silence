for file in ./*.wav
do
    ffmpeg -y -i "$file" -af "areverse,silenceremove=start_periods=1:detection=peak:start_threshold=0.0001,aformat=dblp,areverse" "${file%.*}-temp.wav"
    rm "${file%.*}-temp.wav"
done