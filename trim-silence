for file in ./*.wav
do
    cp "$file" "${file%.*}-temp.wav"
    ffmpeg -y -i "${file%.*}-temp.wav" -af "areverse,silenceremove=start_periods=1:detection=peak:start_threshold=0.0001,aformat=dblp,areverse" "$file"
    rm "${file%.*}-temp.wav"
done